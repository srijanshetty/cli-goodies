#
# Upgrade everything
#
# Authors:
#   transfer.sh
#

# Linux updates
sudo apt-get update && sudo apt-get -y upgrade

# upgrade mr
cd ~ && mr up

# Upgrade github repos
cd $GITHUB_ROOT && mr up

# dotfiles updates
cd $CUSTOM_ROOT && pip install --upgrade peru

# tldr
tldr upate
